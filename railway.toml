[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn nephub.wsgi --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
